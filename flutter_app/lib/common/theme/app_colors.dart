import 'package:flutter/material.dart';

class AppColors {
  // Brand
  static const primary = Color(0xFF4A90E2);
  static const primaryVariant = Color(0xFF357ABD);

  // Neutrals
  static const background = Color(0xFFF5F8FD);
  static const surface = Colors.white;
  static const textPrimary = Color(0xFF1C1C28);
  static const textSecondary = Color(0xFF6E7A8A);

  // Lines / subtle UI
  static const border = Color(0xFFDCE3F0);
  static const muted = Color(0xFFB5C2D6);

  // States
  static const error = Color(0xFFE57373);
  static const success = Color(0xFF4CAF50);

  // Shadows
  static Color softShadow = Colors.blue.shade100.withOpacity(0.4);
}